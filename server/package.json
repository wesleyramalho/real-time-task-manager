{
  "name": "server",
  "version": "1.0.0",
  "description": "Server to simulate a real time application based on Trello UI",
  "main": "server-app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Wesley Ramalho",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "socket.io": "^2.0.4"
  }
}
